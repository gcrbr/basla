from distutils.core import setup

setup(
    name = 'Basla',
    version = '1.0',
    description = 'Python module that allows Tor to be used for sockets and HTTP requests',
    author = 'Giancarlo B.',
    install_requires = ['PySocks']
)